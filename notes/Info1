Why Go (Golang) is Different from Other Languages?
Built in Concurrency
Fast Compilation
Simple Syntax
High Performance
Garbage Collection
Automatically manages memory.
No need to manually free memory.


// ********* 2nd info ******************//
Basic types
Go's basic types are

bool

string

int  int8  int16  int32  int64
uint uint8 uint16 uint32 uint64 uintptr

byte // alias for uint8

rune // alias for int32
     // represents a Unicode code point

float32 float64

complex64 complex128
The example shows variables of several types, and also that variable declarations may be "factored" into blocks, as with import statements.

The int, uint, and uintptr types are usually 32 bits wide on 32-bit systems and 64 bits wide on 64-bit systems. When you need an integer value you should use int unless you have a specific reason to use a sized or unsigned integer type.



note:
%T (Type): This prints the Go data type of the variable.
%v (Value): This prints the default value of the variable in a natural format.
e.g fmt.Printf("Type: %T Value: %v\n", z, z)


// Important git command
git add stuff..
git commit -m "xyz"
git push -u origin main